# @roboratory/fs-tools

Filesystem utilities for experiments and apps.

APIs:
- `ensureDir(path)`
- `listFilesRecursive(root)`
- `hashFile(filePath)`
- `moveFileSafe(src,dest)`
- `copyFileSafe(src,dest)`
- `sanitizeFilename(name)`
