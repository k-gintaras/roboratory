{
  "name": "roboratory-scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:api": "ts-node tests/test-real-api.ts",
    "build": "tsc -p ."
  }
}
