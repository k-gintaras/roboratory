[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "roboratory-robot-data"
version = "0.1.0"
description = "Data-processing robot for Roboratory experiments"
requires-python = ">=3.9"
