{
  "name": "tagged-music",
  "version": "1.0.0",
  "description": "Music tagging system with SQLite database",
  "main": "music-tagger.ts",
  "scripts": {
    "setup": "ts-node setup-demo.ts",
    "upload": "ts-node upload-and-tag.ts",
    "dev": "ts-node setup-demo.ts",
    "build": "tsc",
    "test": "jest --passWithNoTests"
  }
}
